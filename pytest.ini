[pytest]
addopts = -v --tb=short --alluredir=allure-results --strict-markers
testpaths = tests
markers =
	smoke: critical smoke coverage checks
	orangehrm: OrangeHRM-specific test coverage
	regression: full regression suite coverage
